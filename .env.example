# GridGuard optional configuration
#
# Critical alarm notifier webhook endpoint.
# If this variable is not provided, GridGuard uses its
# built-in local development endpoint.
#
# Note:
# GridGuard currently reads this value from the process
# environment. The .env file is not loaded automatically.

GRIDGUARD_WEBHOOK_URL=http://127.0.0.1:9001/notify